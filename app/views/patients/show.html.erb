<h2>Name: <%= @patient.name %></h2>
<h2>Age :<%= @patient.age %></h2>

<ul>
  <% @patient.appointments.each do |appt| %>
    <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
    <li><%= appt.appointment_datetime.readable_appointment_datetime %></li>
    <br>
  <% end %>
</ul>
